name : "SPHERE"
dataset : "experiments/datasets/3D/SPHERE/graph.g2o"
ground_truth : "experiments/datasets/3D/SPHERE/GT.txt"
output : "experiments/results/SPHERE.txt"
visualize : 0
canonic_inliers : 2450
fast_reject_th : 6.251
fast_reject_iter_base : 50
slow_reject_th : 6.251
slow_reject_iter_base : 100

# WORKS AS IS
# --- params required by this IPC code version (S0.2/S1; see TASKS.md) ---
s_factor : 3              # IPC scaling factor S=3 (IPC paper / thesis Ch.4, all experiments). TACO uses 10(2D)/30(3D).
use_best_k_buddies : false
k_buddies : 0
use_recovery : false
