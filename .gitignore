# Build artifacts
build/
.vscode/

# Reference paper PDFs — kept locally, not tracked (large; numbers extracted into docs/IPC.md + docs/TACO.md)
docs/*.pdf

# NOTE: experiments/datasets/ (clean source graphs, ~2 MB of g2o/text) IS tracked — small enough to commit.
# …but generated spoiled data is NOT tracked (regenerable, large):
experiments/datasets/**/SPOILED_DATA/

# Experiment outputs — track ONLY the small metric files (.PR ~0.1 MB, .TE ~0.2 MB) for portability;
# ignore bulky trajectory dumps (.TRJ ~185 MB), scratch dirs, and logs (all regenerable).
experiments/results/**
!experiments/results/**/
!experiments/results/**/*.PR
!experiments/results/**/*.TE
!experiments/results/.gitkeep
*.tum
# stray run outputs anywhere (belt-and-suspenders against root clutter)
/res.txt
/res.PR
**/*_est.txt
**/*_estimate.txt
# g2o linear-solver debug dump (Octave sparse-matrix); auto-written to cwd on a degenerate
# system (e.g. FR079's indefinite info matrix). Diagnostic byproduct, never an input.
debug.txt
**/debug.txt
# stray dumps written to cwd by the gtsam/g2o testers
init.txt
**/init.txt
refined.txt
**/refined.txt

# Python
__pycache__/
*.pyc
.venv/

# OS / editor
.DS_Store

# NOTE: scripts/ IS tracked (our generators live there) — unlike upstream IPC's .gitignore.
