# RUN_LOG — one line per session (date · step · key number · next)

See `TASKS.md` for the plan (stages S0–S6, gates G1–G3).

---

## 2026-06-09

- **S0-0 — Fork + superbuild scaffold.** IPC → `ipc/`; RobustOptimizationSLAM vendored → `baselines/` (`62b1c9c`); root superbuild CMake; `.devcontainer/` (g2o + GTSAM 4.2.0 + Kimera-RPGO). Next: S0.1.
- **S0.1 — Build GREEN + IPC runs. ✅** Devcontainer image built; full superbuild compiles & links: IPC (3) + 24 g2o baselines + 8 GTSAM baselines incl. **gtsam_PCM_2D/3D** + evaluator. IPC smoke-run on clean MIT → **Precision=1, Recall=1**, max consistent set=20, ~7 ms/test, `res.{txt,PR}` written.
  - Build fixes found by building (all build-portability, no behavior change): g2o → `/usr/local` (dropped hard-coded `slam-emix` path); yaml-cpp `::` alias (0.7); SuiteSparse `cs.h` include dir (IPC + RRR); TBB CONFIG mode (oneTBB vs vendored FindTBB); RRR `std::make_unique` qualify. All baseline patches logged in `baselines/VENDOR.md`.
  - Config: IPC code needs 4 keys absent from the (inconsistent) upstream cfgs — added `s_factor`/`use_best_k_buddies`/`k_buddies`/`use_recovery` to all 12 cfgs. `s_factor=1.0` (neutral) is a **placeholder** — confirm the paper/author value for exact reproduction.
- **S0.2 — Experiment scaffold. ✅** `experiments/{datasets,configs,results,scripts}` + `run_method.sh` (per-tool CLI dispatch) + README. `canonic_inliers` verified per dataset (CSAIL 128, INTEL 256, M3500 1954, MIT 20, FRH 1505). Moved clean **source** datasets root `datasets/` → `experiments/datasets/` (cfgs + docs repointed; generated spoiled data will go to `experiments/results/`). IPC re-verified on new path (P=R=1).
- **Paper references extracted. ✅** Read IPC paper + Olivastri PhD thesis → `docs/IPC.md` and `docs/TACO.md` (numbers kept in **separate** files per method, IPC vs TACO). Confirmed params: **IPC `s_factor` = 3** (all experiments; was placeholder 1.0 — now fixed in all cfgs, re-verified P=R=1); IPC has **no k** (all edges must agree ⇒ `use_best_k_buddies: false` ✓); χ² uses α=0.95 (DoF 3/2D, 6/3D), fast/slow 6.251/11.345 are code-level. TACO (thesis Ch.5, "Test And Check Optimization") uses s=10(2D)/30(3D), k default 2; **TACO is correlation-BLIND** (per-loop decision) — so it should also fail on correlated outliers (supports G1/our gap).
- **S1 in progress — IPC reproduction pipeline built + validated on M3500.**
  - Tooling (all author-faithful, paths-only changes): `experiments/scripts/generate_spoiled.py` (Vertigo, n=rate×inliers, seeds 0–9, deterministic/reusable), `experiments/scripts/ipc_experiments_2D.sh` (minimal-edit copy of the author's driver; `yq` overrides), `experiments/scripts/aggregate_pr.py`. Installed `yq` (in Dockerfile).
  - **Found:** `k_buddies`/`use_recovery` are TACO params but **INERT in the IPC binary** → see `docs/parameters_ipc_vs_taco.md`. The author's committed script uses **s=10** ("IPC_S10", thesis), **not** the 2024 paper's **s=3**.
  - **Ran full M3500 sweep at s=10** (10 rates ×10 runs, ×10 parallel): precision **0.98–0.996**, recall **0.61–0.77**, F1 0.75–0.87 (`experiments/results/IPC/M3500/.../metrics_summary.csv`). Machinery validated. But recall <80% — this is the **thesis IPC_S10 operating point**, NOT the paper.
  - **NEXT:** rerun M3500 at **s=3** to verify the **2024 paper** (recall should exceed 80%, F1 → ~0.86+), then extend to all 6 datasets, then S2 (TACO). Migrating to a 52-core/64GB server for the full campaign — regenerate spoiled data there (deterministic), raise parallelism (result-neutral). See `HANDOFF.md`.

## 2026-06-10 (server: 52-core Xeon 6230R / 62 GB, devcontainer `vsc-robust_pgo`)

- **Server migration — spoiled data regenerated + build green.** Working inside the running devcontainer (repo bind-mounted at `/workspaces/robust_pgo`; deps g2o/GTSAM/TBB/yq in image; build/ writes back to host).
  - **Spoiled data regenerated on server (deterministic)** for 5 datasets (CSAIL 128, INTEL 256, M3500 1954, MIT 20, FRH 1505) → `experiments/datasets/2D/<DS>/SPOILED_DATA/` (100 files each, 500 total; gitignored). Author-faithful `generate_spoiled.py` (unmodified `generateDataset.py`, n=round(rate/100·inliers), rates 10–100, seeds=run 0–9). Verified: outlier counts match formula (M3500 195/977/1954 at 10/50/100%); regen of rate10/run00 **byte-identical** ⇒ determinism confirmed. **FR079 still blocked** (TORO `.graph`, inliers TBD — not guessed).
  - **Superbuild GREEN in container** (`cmake -DCMAKE_BUILD_TYPE=Release && make -j52`, exit 0): IPC (ipc_tester_2D/3D, graph_fixer) + 33 g2o baselines. GTSAM/PCM tier off (opt-in later). Smoke test IPC on clean MIT → **P=R=1**, max set=20, ~12 ms/test; run banner shows **S=3 | TH=6.251** (paper s_factor=3 confirmed in cfgs).
  - **All 6 author datasets now staged.** Spoiled data generated on server for all 6 (added **FR079, inliers=229**); inlier counts derived as loop-closure edges (`b≠a+1`) — method matches all 5 known values exactly. Created missing IPC configs **FRH (1505)** + **FR079 (229)** in `ipc/cfg/2D/` (mirroring M3500, s=3, th 6.251/11.345). Smoke (clean): FRH P=R=1 ✓; M3500/MIT P=R=1 ✓.
  - **Downloaded the IPC paper** → `docs/2024_Olivastri_IPC...pdf` (gitignored); re-confirmed from source: 6 datasets incl. **FRH + FR079** (Table II + Fig.1 is FR079@70%), s=3 all experiments, rates 10–100, 10 runs, inliers = clean loop-closure edges.
  - **FR079 anomaly investigated + resolved as EXPECTED (see HANDOFF fact #6).** Clean FR079 recall = **0.33** (76/229). Info matrices look TORO-ordered to g2o (`q_θθ=0` on all 1217 edges). **NOT converted** — file is **byte-identical to the author's release Drive copy** (vertices+edges verified). The author ran this exact file → we replicate as-is; low recall is absorbed into the paper's 6-dataset average (per-dataset recall unpublished). If S1 averaged recall < paper's >80%, revisit FR079 first — but never edit the dataset.
  - **Per-run timing measured:** M3500 rate-50 s=3 = **557 s wall, single-threaded** (user/real≈1.0). Cap-26 ⇒ M3500 ≈ 4 rolling waves ≈ ~35 min; full IPC@s=3 6-dataset campaign ≈ ~1–1.5 h.
  - **Baselines built + audited against the paper's 7 comparators.** Rebuilt with `-DBUILD_GTSAM_BASELINES=ON` → GTSAM tier (gtsam_{DCS,GNC,HUBER,PCM}) compiles; PCM smoke-runs (emits own `.PR`). Found gtsam cfg missing `init_loop` (add to baseline cfgs). **Source audit:** of the paper's 7 (GNC,ADAPT,MAXMIX,DCS,GM,HUBER,PCM), the released code has 5; **GM and ADAPT were NOT released** (GM absent; ADAPT appears only as a label in `ipc/scripts/plotATERPET.py` — the author ran it via a private g2o patch). i.e. the comparison is real but its code is incompletely released (like TACO).
  - **Added GM comparator (library kernel, not a reimplementation).** GTSAM + g2o both already ship Geman-McClure. Created `baselines/robust_gtsam/src/gm_2D/3D.cpp` (verbatim DCS clone, only kernel swapped to `mEstimator::GemanMcClure`) + `gtsam_GM_2D/3D` targets. Builds ✓; GM ≡ DCS on clean MIT (P=1) as expected. Logged in `baselines/VENDOR.md` (#7). GM param `th=Chi2inv(alpha,dof)` — tune for S3.
  - **ADAPT — reimplemented + verified functional.** Confirmed unavailable anywhere reusable (not in GTSAM/g2o; **Ceres not installed and has no Barron loss anyway**; AEROS/Chebrolu code unreleased; Barron's own code is ML-only). Reimplemented in the gtsam tier: `robust_gtsam/include/barron.hpp` (Barron loss/weight mEstimator + MAD scale + α grid-MLE w/ truncated partition) + `src/adapt_2D/3D.cpp` (inner GTSAM IRLS for fixed α inside an outer loop adapting α & c from residuals). Builds ✓. **M3500@30% → α→-2, P=R=1** (kept all 1954 inliers, rejected all 586 outliers — not trivial). Logged VENDOR.md (#8). ⚠️ **S3/G1: verify vs the paper's ADAPT numbers** (α grid/partition/scale are pragmatic) — treat like TACO (reimpl pending verification).
  - **✅ All 7 IPC-paper comparators build & SMOKE-TESTED:** IPC (P=R=1), PCM (P=1), GNC (P=1,R=.65), DCS (P=1), GM (P=1), MAXMIX (P=1,R=.4), ADAPT (M3500@30% P=R=1) — all run & emit metrics. GM+ADAPT are our additions.
  - ⚠️ **Known issue: gtsam HUBER crashes on MIT** (`IndeterminantLinearSystemException` — GaussNewton diverges on MIT's poor init; Huber applied to all edges, no LM damping). gtsam HUBER is FINE on INTEL/CSAIL/M3500 (P=R=1); g2o-tier `HUBER_2D` handles MIT fine (P=1,R=.95). **S3 fix:** use LM (not GN) in `huber_2D.cpp`, or use the g2o-tier HUBER for MIT — do NOT silently change the vendored optimizer before deciding. Not blocking S1.
  - Note: gtsam baselines need `init_loop` cfg key (added in smoke tests); g2o baselines use `-cfg` + `params_2D.yaml`. Baseline param tuning (GM/ADAPT/PCM/MAXMIX "best fixed set") + ADAPT verification still pending for S3/G1.
  - **Autonomous S1 runner built + LAUNCHED.** `experiments/scripts/run_ipc_campaign.sh`: worker pool (CAP=26), idempotent skip (valid `.PR` ⇒ resume after crash/power-loss), atomic output (scratch → mv on success), per-job scratch CWD (isolates debug.txt/init.txt). Validated on MIT (runs + resume-skip both confirmed). Created `experiments/results/` (+ `.gitkeep`). Layout: `results/IPC/<ds>/<date>/IPC_S3/<rate>/<run>.{TRJ,PR}` (matches `aggregate_pr.py`). **Launched full S1**: IPC, 6 datasets × 10 rates × 10 runs = 600, s=3, detached in container (`docker exec -d`), log `results/IPC/campaign_260610.log`. ETA ~1–1.5 h (M3500/FRH ~9 min/run dominate).
  - **NEXT:** when campaign done → aggregate per dataset, compute 6-dataset-averaged recall, **diff vs `docs/IPC.md` (paper recall >80%, F1≈0.91@50/0.89@100)** = the S1 verdict. Watch FR079 (expected low, fact #6). Then S2 (TACO).
